# Copy to .env.local for local development, or set in the Netlify site's
# environment variables for production. Never commit a real .env* file —
# .gitignore already excludes them; this example.env file is the reference.

# Canonical production URL, used for metadataBase, Open Graph, and canonical tags.
NEXT_PUBLIC_SITE_URL=https://lumenfoundry.net
